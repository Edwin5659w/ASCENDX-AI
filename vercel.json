{
  "buildCommand": "cd .. && npm run build --workspace=@ascendx/shared && cd web && npm ci && npm run build",
  "outputDirectory": "web/dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
