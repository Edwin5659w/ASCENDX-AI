services:
  - type: web
    name: ascendx-api
    env: node
    buildCommand: npm ci && npm run build --workspace=@ascendx/shared && npm run build --workspace=backend
    startCommand: npm run start --workspace=backend
    healthCheckPath: /health
